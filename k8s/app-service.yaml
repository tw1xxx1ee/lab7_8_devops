apiVersion: v1
kind: Service
metadata:
  name: app-service
  labels:
    app: my-app
    release: prometheus
spec:
  type: NodePort
  selector:
    app: my-app
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30000
